# dual_a-ba_edu 직접 의존성 (Python 3.12 기준)
# lerobot extras: feetech=Feetech 모터 SDK, viz=rerun 시각화(--display_data),
# dataset=에피소드 영상 인코딩(av/torchcodec), smolvla=SmolVLA 파인튜닝 의존성
lerobot[feetech,viz,dataset,training,smolvla]==0.6.0  # training=accelerate/wandb(lerobot-train 필수), smolvla=transformers/num2words
numpy==2.2.6  # 수치 계산 (카메라 헤드 궤적)
pynput==1.8.2  # record 키보드 에피소드 제어 + keyboard 헤드 모드
mujoco==3.12.0  # SO-101 시뮬레이션 뷰어·디지털 트윈 (src/mujoco_sim)
pyzmq==27.1.0  # 라즈베리파이(host) ↔ PC(client) 통신 (leader_teleop/host.py)
