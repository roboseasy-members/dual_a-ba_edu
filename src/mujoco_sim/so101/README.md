# SO-101 MuJoCo 모델 (vendored)

출처: [TheRobotStudio/SO-ARM100](https://github.com/TheRobotStudio/SO-ARM100)
저장소의 `Simulation/SO101/` — 수정 없이 그대로 가져왔습니다.
라이선스는 Apache-2.0이며 원문을 이 폴더의 `LICENSE`로 동봉합니다.

포함 파일: `scene.xml`(진입점), `so101_new_calib.xml`(로봇 본체 MJCF),
`assets/*.stl`(메시 13개, 약 16MB).

모델을 갱신하려면 원 저장소에서 위 파일들을 다시 복사하면 됩니다.
